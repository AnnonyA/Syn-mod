diff --git a/saveinstance.luau b/saveinstance.luau
--- a/saveinstance.luau
+++ b/saveinstance.luau
@@ -6361,6 +6361,8 @@
 		local fileTarget = not CopyToClipboard and not Callback
 		checks.fileTarget = fileTarget
 		local valid = checks.nonEmpty and checks.chunkSizeMatches and checks.formatEnvelope
+		checks.fileWritable = not fileTarget or writefile ~= nil
+		valid = valid and checks.fileWritable
 
 		if fileTarget then
 			if isfile then
